{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "9.3.0",
    "@sap/html5-repo-mock": "2.0.1"
  },
  "scripts": {
    "build-ui": "cd ui && if [ ! -d ${workspaceFolder}/reactstandalone/ui/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/reactstandalone/node_modules/react-scripts ]; then npm i; fi && npm run build"
  }
}
